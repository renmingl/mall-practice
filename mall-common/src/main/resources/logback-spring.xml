<?xml version="1.0" encoding="UTF-8"?>
<!--
  mall-common 统一日志配置（场景 12.9）
  输出策略：开发期控制台；运行期滚动文件（INFO/ERROR 分离，按天滚动 + 保留 15 天，各服务独立日志文件）
  - 日志目录：logs/{spring.application.name}/，各服务自动按应用名分目录
  - pattern 统一携带 [traceId:%X{traceId}]，跨服务 grep 同一 traceId 串起整条调用链
  注意：日志脱敏（手机号/密码/token 不打印全量）由代码层约定，配置层不做
-->
<configuration>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="mall"/>
    <property name="LOG_DIR" value="logs/${appName}"/>
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %5p [%thread] [traceId:%X{traceId}] %logger{36} - %msg%n"/>

    <!-- 控制台：开发期主力输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- INFO 文件：按天滚动，保留 15 天 -->
    <appender name="INFO_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/info.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/info.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>15</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ERROR 文件：仅 ERROR 级别，按天滚动，保留 15 天 -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/error.log</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/error.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>15</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="INFO_FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>

</configuration>
